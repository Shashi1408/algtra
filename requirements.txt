# Zerodha Intraday Trading System — Python dependencies
# Install with:  pip install -r requirements.txt

# Zerodha broker API
kiteconnect>=4.2.0

# Data & numerics
pandas>=2.0
numpy>=1.24

# NSE live data — paper/dry-run fallback (pulls directly from NSE India website)
# Used instead of yfinance which is no longer reliable
nsetools>=2.0.1

# Machine learning
scikit-learn>=1.3

# Scheduling & CLI
schedule>=1.2
colorama>=0.4
tabulate>=0.9
